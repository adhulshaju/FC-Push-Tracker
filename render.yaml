services:
  - type: web
    name: fc-push-tracker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: GEMINI_API_KEY
        sync: false   # you'll paste your free key in Render dashboard
